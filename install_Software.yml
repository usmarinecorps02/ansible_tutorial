---
  - hosts: all
    become: true
    tasks:

      - name: Update repository index
        yum:
          update_cache: yes

      - name: Install apache2 and PHP packages
        yum:
          name:
            - httpd
            - php
          state: latest

