---
  - hosts: all
    become: true
    tasks:

      - name: Update repository index
        yum:
          update_cache: yes

      - name: Install apache2 package
        yum:
          name: httpd
          state: latest

      - name: Install PhP package
        yum:
          name: php
          state: latest
